WILL UPDATE BEFORE BETA

- if using db helper with new service make sure to include: ef and ef relational nuget packages to that project, pass in
  the db conn string to container as env var then pass that into db contructor
